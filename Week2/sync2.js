function sum(n) {
    let s=0;
    for(let i=1;i<=n;i++)
    {
       s+=i;
    }
    return s;
}
console.log(sum(10));

console.log(10*(10+1)/2);

// any line being run by the thread one at a time is called synchronus
